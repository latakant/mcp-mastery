# MCP Auth — Bearer token clients must send in Authorization header
MCP_AUTH_TOKEN=your-secret-token-here

# Server port (default: 3000)
PORT=3000

# SQLite database path (default: ./data/dev.db)
DB_PATH=./data/dev.db
